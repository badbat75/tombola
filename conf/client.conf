# Tombola Client Configuration

# Server host address to connect to
host = 127.0.0.1

# Server port number to connect to
port = 3000

# Optional: Connection timeout in seconds
# timeout = 30

# Optional: Retry attempts on connection failure
# retry_attempts = 3

# Optional: Client name (can be overridden by command line arguments)
# client_name = DefaultClient
